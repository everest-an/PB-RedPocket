version: '3.8'

services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - ENV=production
      - DATABASE_URL=postgres://postgres:${DATABASE_PASSWORD:-redpocket_secret}@postgres:5432/redpocket?sslmode=disable
      - REDIS_URL=redis://redis:6379
      - RPC_URL=${RPC_URL:-https://mainnet.base.org}
      - CHAIN_ID=8453
      - USDC_ADDRESS=0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913
      - BUNDLER_URL=${BUNDLER_URL:-https://api.pimlico.io/v2/8453/rpc?apikey=pim_PuGohRASMe6FsMfoB9gx6N}
      - PAYMASTER_URL=${PAYMASTER_URL:-https://api.pimlico.io/v2/8453/rpc?apikey=pim_PuGohRASMe6FsMfoB9gx6N}
      - ENTRY_POINT_ADDRESS=0x5FF137D4b0FDCD49DcA30c7CF57E578a026d2789
      - VAULT_ADDRESS=${VAULT_ADDRESS:-0x66794fC75C351ad9677cB00B2043868C11dfcadA}
      - JWT_SECRET=${JWT_SECRET:-change-me-in-production}
      - RATE_LIMIT_RPS=1000
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=redpocket
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=${DATABASE_PASSWORD:-redpocket_secret}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
